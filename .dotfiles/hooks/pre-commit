#!/usr/bin/env bash

FILES=$(git --git-dir=$HOME/.dotfiles --work-tree=$HOME diff --cached --name-only)

for file in $FILES; do
  case "$file" in
    .ssh/*|.gnupg/*|.aws/*|.kube/*|.gcloud/*|*.pem|*.key|*.env*)
      echo "Blocked sensitive file: $file"
      exit 1
      ;;
  esac
done
